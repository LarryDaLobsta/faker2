version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "../../migrations/*.up.sql"
    queries:
      - "queries/*.sql"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        sql_package: "pgx/v5"